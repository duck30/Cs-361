
# Cs-361

I created a virtual environment  
In Visual Studio Code terminbal in typed:  py -3.12 -m venv .venv  
In order to use flask I then installed: pip install flask  
Python has built in support for SQlite so i installed : pip3 install flask-mysqldb  
If database included does not load when running ap.py, I ran createDatabase.py to create local database  
Then I ran app.py which shpuld now show all data from local database. 
